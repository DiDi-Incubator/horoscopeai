# /v2/log

***
    a <- @input
    b <- $delta
    $delta <- 1
    <> {
        ? positive = a > 0
        => {
            output <- "positive"
        }

        ? negative = a < 0
        => {
            output <- "negative"
        }

        ? zero = true
        => {}
    }

    result <- 1
    <include> => /v2/log-include?input=@input & tag="negative"
    export <- include->tag
    <schedule> + => /v2/log-schedule?input=@input

***