# /v2/entry

***
    result <- @level
    // event_id <- @event_id
    // lazy <~ 5
    // <> {
    //     ? flag = true
    //     => {
    //         count <- 10
    //     }
    // }
    <include> => /v2/base?input=@level&tag="default"
    <schedule> + => /v2/base?input=@level&tag="default"
***