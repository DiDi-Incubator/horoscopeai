# /v2/trace

* Wait
``` wait
```

***
    <> {
        ? init = $count == null !! true
        => {
            $count <- 0
        }
        
        ? add = @flag
        => {
            $count <- $count + $delta
        }
    }
    
    Wait()
    result <- $count
***
