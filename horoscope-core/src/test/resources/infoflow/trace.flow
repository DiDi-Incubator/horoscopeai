# /test/trace
* mode = "compatible"

* Wait
``` wait
```

***
    <> {
        ? init = $count == null
        => {
            $count <- 0
        }
        
        ? add = @flag
        => {
            $count <- $count + $delta
        }
    }
    
    Wait()
    result <- $count
***
